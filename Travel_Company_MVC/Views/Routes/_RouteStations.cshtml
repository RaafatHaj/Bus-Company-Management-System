@using TravelCompany.Domain.Entities

@model RouteStationsViewModel

	<div class="ps-7">

	<div class="  border-0 ">

		<div>
			<div class="d-flex">

				<h5>
					Route Stations
				</h5>
				<span class="bullet   bullet-dot  bg-success ms-2 mt-2"></span>
			</div>

			<span class="text-muted ">All staions of selected route.</span>

		</div>




		<!--animate__animated animate__flipInX -->
		@* <div class="d-flex align-items-center justify-content-between mx-6">

			<div class="">

				<h3 class="d-flex flex-column  align-items-start ms-3 ">

					<span class="fw-bold mb-2 text-gray-900">@Model.RouteName</span>
					<span class="text-muted fw-semibold fs-7">@Model.EstimatedDistance km</span>



				</h3>

			</div>

			<div class="d-flex flex-column  align-items-start me-5">


				<div>
					<a href="apps/user-management/users/view.html"
					   class="text-gray-800 text-hover-primary mb-2 ms-2">@Model.Stations.Count<span> station</span></a>
				</div>
				<div class="badge badge-light fw-bold">@TimeSpan.FromMinutes(Model.EstimatedTime).ToString(@"hh\:mm") hours</div>

			</div>
		</div>
 *@

	</div>
	<!--end::Header-->
	<!--begin::Body-->
	<div class="d-flex  ps-5 pt-10  ">
		<!--begin::Timeline-->
		<div class="timeline-label">

			@for (int i = 0; i < Model.Stations.Count; i++)
			{


				var pointColor = "text-warning";

				@* 	if (Model[i].PointOrder == 1)
			pointColor = "text-danger";
			else if (Model[i].PointOrder == Model.Count)
			pointColor = "text-success";
			else pointColor = "text-warning"; *@






				<!--begin::Item-->
				<div class="timeline-item ">
					<!--animate__animated animate__flipInX-->
					<!--begin::Label-->
					<div class="timeline-label fw-bold text-gray-800 fs-6">
						@TimeSpan.FromMinutes(Model.Stations[i].StationTimeValue).ToString(@"hh\:mm")
						<span class="fw-mormal timeline-content  text-gray-500 fs-6">hours</span>
					</div>
					<!--end::Label-->
					<!--begin::Badge-->
					<div class="timeline-badge">
						<i class="fa fa-genderless  @pointColor fs-1"></i>
					</div>
					<!--end::Badge-->
					<!--begin::Text-->
					@*
				<a href="#"
				class="fs-6 text-gray-800 fw-bold d-block text-hover-primary">@Model.LastStation.StationName</a> *@

					<a href="#" class="fw-mormal timeline-content fw-bold text-gray-500 fs-6 ms-2">
						@Model.Stations[i].StationOrder.<span class="text-gray-900 fw-bold text-hover-primary ">@Model.Stations[i].StationName</span>
					</a>
					<!--end::Text-->
				</div>
				<!--end::Item-->
			}

		</div>
		<!--end::Timeline-->
	</div>
	<!--end: Card Body-->


	</div>


